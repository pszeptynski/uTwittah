<?php

// sprawdz czy formularz POST
// jest tak to znajdz usera o danym mailu
// sprawdz czy wpisane haslo ($_POST['password']) jest poprawne za pomoca password_verify
// jesli user i pass sa ok - zaloguj usera
// logowanie to wpisanie do sejsji id usera, np. jako $_SESSION['logged_user_id']
// po udanym zalogwoaniu przenies na stronie główną: header('location: main.php');

// jesli logowanie sie nie powiodlo, wyswietl info, ze login lub haslo sa bledne

// wyswietl form logowania z polami email i pass (action="#", method="post")

// wyswietl link do rejestracji usera

